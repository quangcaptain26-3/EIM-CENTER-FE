# Biến môi trường chạy local (Development)
# Khớp backend: PORT=3000 (eim-center-backend/.env), prefix API /api/v1
VITE_API_URL=http://localhost:3000/api/v1
VITE_APP_NAME=EIM Center
VITE_APP_ENV=development

# (Tùy chọn thêm nếu cần sau này)
# VITE_API_TIMEOUT=30000
